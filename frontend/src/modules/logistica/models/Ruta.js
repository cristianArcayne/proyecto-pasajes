export class Ruta {
  constructor({ id_ruta, origen, destino, precio_ruta }) {
    this.id_ruta = id_ruta;
    this.origen = origen;
    this.destino = destino;
    this.precio_ruta = precio_ruta;
  }
}
